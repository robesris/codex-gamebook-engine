*.json
!package.json
!package-lock.json
!codex.schema.json
node_modules/
01fftd.xml
books
books/
# Project-specific dev-process artifacts, playbook runs, session logs,
# and walkthroughs. These are useful locally but not intended for
# version control in this public repo. If any of their contents are
# ever promoted to general reference material, move them to a new path
# (examples/, docs/, etc.) rather than re-adding plans/.
plans/
HANDOFF.md
